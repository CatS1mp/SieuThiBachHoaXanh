@model BachHoaXanh.ViewModels.ProductDetailViewModel
@{
    ViewData["Title"] = "Sản phẩm " + Model.Product.ProductName;
}
<link rel="stylesheet" href="~/css/product-detail.css">
<style>
    .review-form {
        background: #f9f9f9;
        border-radius: 8px;
        padding: 20px;
        margin: 20px 0;
        border: 1px solid #e0e0e0;
    }

    .rating-field, .comment-field {
        margin-bottom: 15px;
    }

        .rating-field label, .comment-field label {
            display: block;
            font-weight: 600;
            margin-bottom: 8px;
            color: #333;
        }

    .star-rating {
        display: inline-flex;
        flex-direction: row-reverse;
        margin-bottom: 10px;
    }

        .star-rating input {
            display: none;
        }

    .star-label {
        font-size: 28px;
        color: #ddd;
        cursor: pointer;
        transition: color 0.2s ease;
        margin-right: 5px;
    }

    .star-rating input:checked ~ label,
    .star-rating label:hover,
    .star-rating label:hover ~ label {
        color: #ffd700;
    }

    .star-rating input:checked + label {
        color: #ffd700;
    }

    textarea {
        width: 100%;
        padding: 12px;
        border: 1px solid #ddd;
        border-radius: 6px;
        resize: vertical;
        font-family: inherit;
        font-size: 14px;
        transition: border-color 0.3s;
    }

        textarea:focus {
            border-color: #4caf50;
            outline: none;
            box-shadow: 0 0 0 2px rgba(76, 175, 80, 0.2);
        }

    .submit-review {
        background: #4caf50;
        color: white;
        border: none;
        padding: 10px 20px;
        border-radius: 6px;
        font-weight: 600;
        cursor: pointer;
        transition: background 0.3s;
        margin-top: 10px;
    }

        .submit-review:hover {
            background: #45a049;
        }

    @@media (max-width: 768px) {
        .star-label {
            font-size: 24px;
        }
    }
    /* Additional CSS for rating section */
    .rating-section {
        margin-top: 40px;
        padding: 20px 0;
    }

    .rating-summary {
        background: #fff;
        border: 1px solid #e5e5e5;
        border-radius: 8px;
        padding: 30px;
        margin-bottom: 30px;
    }

    .rating-overview {
        display: grid;
        grid-template-columns: 1fr 2fr;
        gap: 40px;
        align-items: center;
    }

    .average-rating {
        text-align: center;
    }

    .average-score {
        font-size: 3rem;
        font-weight: bold;
        color: #333;
        margin-bottom: 10px;
    }

    .stars {
        display: flex;
        justify-content: center;
        gap: 2px;
        margin-bottom: 10px;
    }

    .star-rating {
        display: inline-block;
        direction: rtl; /* Để hover từ phải sang trái */
    }

        .star-rating input {
            display: none;
        }

        .star-rating label {
            font-size: 24px;
            cursor: pointer;
            display: inline-block;
            width: 24px;
            height: 24px;
        }

            .star-rating input:checked ~ label,
            .star-rating label:hover,
            .star-rating label:hover ~ label {
                color: #ffd700;
            }

    .star {
        width: 20px;
        height: 20px;
        color: #ffd700;
    }

        .star.filled {
            fill: #ffd700;
        }

        .star.empty {
            fill: #e5e5e5;
        }

    .total-reviews {
        color: #666;
        font-size: 14px;
    }

    .rating-distribution {
        display: flex;
        flex-direction: column;
        gap: 12px;
    }

    .rating-row {
        display: flex;
        align-items: center;
        gap: 12px;
    }

    .rating-label {
        display: flex;
        align-items: center;
        gap: 4px;
        width: 50px;
        font-size: 14px;
    }

    .rating-bar {
        flex: 1;
        height: 8px;
        background: #f0f0f0;
        border-radius: 4px;
        overflow: hidden;
    }

    .rating-fill {
        height: 100%;
        background: #ffd700;
        transition: width 0.3s ease;
    }

    .rating-count {
        width: 30px;
        text-align: right;
        font-size: 14px;
        color: #666;
    }

    .reviews-list {
        display: flex;
        flex-direction: column;
        gap: 20px;
    }

    .review-item {
        background: #fff;
        border: 1px solid #e5e5e5;
        border-radius: 8px;
        padding: 20px;
    }

    .review-header {
        display: flex;
        gap: 15px;
        margin-bottom: 15px;
    }

    .review-avatar {
        width: 48px;
        height: 48px;
        border-radius: 50%;
        background: #f0f0f0;
        display: flex;
        align-items: center;
        justify-content: center;
        font-weight: bold;
        color: #666;
    }

    .review-info {
        flex: 1;
    }

    .review-name {
        font-weight: 600;
        margin-bottom: 4px;
        position: relative;
        display: inline-block;
        cursor: pointer;
    }

    .review-date {
        font-size: 12px;
        color: #999;
        margin-bottom: 8px;
    }

    .review-stars {
        display: flex;
        gap: 2px;
    }

        .review-stars .star {
            width: 16px;
            height: 16px;
        }

    .review-comment {
        color: #555;
        line-height: 1.6;
    }

    .pagination a {
        margin: 0 5px;
        padding: 5px 10px;
        text-decoration: none;
        color: #007bff;
    }

        .pagination a.active {
            background-color: #007bff;
            color: white;
            border-radius: 3px;
        }

    /* User Info Tooltip Styles */
    .user-info-tooltip {
        position: absolute;
        top: 100%;
        left: 0;
        z-index: 1000;
        width: 280px;
        background: white;
        border-radius: 8px;
        box-shadow: 0 5px 15px rgba(0,0,0,0.15);
        padding: 15px;
        visibility: hidden;
        opacity: 0;
        transition: opacity 0.3s, visibility 0.3s;
        pointer-events: none;
        border: 1px solid #e5e5e5;
        margin-top: 10px;
    }

    .review-name:hover .user-info-tooltip {
        visibility: visible;
        opacity: 1;
    }

    .user-info-tooltip::before {
        content: '';
        position: absolute;
        top: -8px;
        left: 15px;
        width: 16px;
        height: 16px;
        background: white;
        transform: rotate(45deg);
        border-left: 1px solid #e5e5e5;
        border-top: 1px solid #e5e5e5;
    }

    .user-info-header {
        display: flex;
        align-items: center;
        gap: 10px;
        margin-bottom: 12px;
        padding-bottom: 12px;
        border-bottom: 1px solid #f0f0f0;
    }

    .user-info-avatar {
        width: 50px;
        height: 50px;
        border-radius: 50%;
        background: #f0f0f0;
        display: flex;
        align-items: center;
        justify-content: center;
        font-weight: bold;
        font-size: 20px;
        color: #666;
    }

    .user-info-name {
        font-weight: 600;
        font-size: 16px;
        color: #333;
    }

    .user-info-details {
        display: flex;
        flex-direction: column;
        gap: 8px;
    }

    .user-info-row {
        display: flex;
        align-items: center;
        gap: 8px;
        font-size: 13px;
    }

    .user-info-label {
        color: #666;
        width: 100px;
    }

    .user-info-value {
        color: #333;
        font-weight: 500;
    }

    .user-info-stats {
        display: flex;
        justify-content: space-between;
        margin-top: 12px;
        padding-top: 12px;
        border-top: 1px solid #f0f0f0;
    }

    .user-stat {
        text-align: center;
    }

    .user-stat-value {
        font-weight: 600;
        color: #333;
        font-size: 16px;
    }

    .user-stat-label {
        font-size: 12px;
        color: #666;
    }

    @@media (max-width: 768px) {
        .rating-overview

    {
        grid-template-columns: 1fr;
        gap: 20px;
    }

    .average-score {
        font-size: 2.5rem;
    }

    .user-info-tooltip {
        width: 250px;
        left: -50px;
    }

        .user-info-tooltip::before {
            left: 60px;
        }


    }
</style>

<div class="container-wrapper">
    @await Html.PartialAsync("_SidebarOverplay")
    <div class="breadcrumb-container">
        <div class="breadcrumb-icon d-inline-block">
            <i class="fas fa-chevron-left"></i>
        </div>
        <p>Xem chi tiết sản phẩm <b>#(@Model.Product.ProductID)</b></p>
    </div>

    <div class="product-detail-container">
        <div class="pdetail-left">
            <!-- Product Images -->
            <div class="pimages">
                <div class="main-swiper">
                    <div class="swiper mySwiper">
                        <div class="swiper-wrapper">
                            @foreach (var image in Model.Product.Images)
                            {
                                <div class="swiper-slide">
                                    <a href="#" data-fancybox="gallery">
                                        <img src="~/images/prods/@image.ImagePath" />
                                    </a>
                                </div>
                            }
                        </div>
                        <div class="swiper-item swiper-btn-prev">
                            <i class="fas fa-chevron-left"></i>
                        </div>
                        <div class="swiper-item swiper-btn-next">
                            <i class="fas fa-chevron-right"></i>
                        </div>
                        <div class="swiper-pagination"></div>
                    </div>
                </div>
                <div class="sub-swiper">
                    <div thumbsSlider="" class="swiper mySwiper2">
                        <div class="swiper-wrapper">
                            @foreach (var image in Model.Product.Images)
                            {
                                <div class="swiper-slide">
                                    <img src="~/images/prods/@image.ImagePath" />
                                </div>
                            }
                        </div>
                    </div>
                </div>
            </div>
            <!-- Product Description -->
            <div class="pdescription">
                <p class="label">Thông tin sản phẩm</p>
                <div class="divine"></div>
                <br>
                <p>@Model.Product.Description</p>
            </div>
        </div>

        <div class="pdetail-right">
            <p class="pname">@Model.Product.ProductName</p>
            <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 25px">
                <span class="favorite"
                      onclick="toggleFavorite(@Model.Product.ProductID, this)"
                      data-is-favorited="@Model.Product.isFav.ToString().ToLower()">
                    <img class="icon-fav-detail" src="~/images/@(Model.Product.isFav ? "favor.png" : "unfavor.png")" />
                    @(Model.Product.isFav ? "Đã yêu thích" : "Yêu thích")
                </span>
                <div class="pshare" onclick="showShareMessage()"><i class="fa-solid fa-share-from-square pr-1"></i>Chia sẻ</div>
            </div>

            <form id="cartForm" action="@Url.RouteUrl("them-gio-hang", new { productid = Model.Product.ProductID })" method="post">
                <input type="hidden" name="ProductID" value="@Model.Product.ProductID" />

                @if (Model.Product.StockQuantity > 0)
                {
                    <!-- Quantity Controls -->
                    <div class="qty-controls">
                        <p class="label">Số lượng:</p>
                        <div>
                            <button type="button" class="qtyminus" aria-hidden="true">−</button>
                            <input id="qty" name="quantity" min="1" max="@Model.Product.StockQuantity" step="1" value="1">
                            <button type="button" class="qtyplus" aria-hidden="true">+</button>
                        </div>
                    </div>

                    <p class="kho">
                        Sản phẩm này còn <b><u>@Model.Product.StockQuantity</u></b> sản phẩm trong kho
                    </p>

                    <div class="pprice-container">
                        <p class="label">Giá:</p>
                        <div class="price-controlers">
                            <p class="price main-price">
                                <b>@Model.Product.Price.ToString("N0")<sup><u>đ</u></sup></b>
                            </p>
                        </div>
                    </div>

                    @if (Model.Promotion != null)
                    {
                        <div class="pprice-container">
                            <p class="label">Giảm giá:</p>
                            <div class="price-controlers">
                                @{
                                    var discount = Model.Product.Price - Model.Promotion.NewPrice;
                                }
                                <p class="price text-danger">
                                    -@discount.ToString("N0") <sup><u>đ</u></sup>
                                </p>
                            </div>
                        </div>

                        <div class="pprice-container">
                            <p class="label">Tạm tính:</p>
                            <div class="pprice-controllers">
                                <p id="temporary-price" class="price main-price text-success">
                                    <b>@Model.Promotion.NewPrice.ToString("N0") <sup><u>đ</u></sup></b>
                                </p>
                            </div>
                        </div>
                    }
                    else
                    {
                        <div class="pprice-container">
                            <p class="label">Tạm tính:</p>
                            <div class="pprice-controllers">
                                <p id="temporary-price" class="price main-price">
                                    <b>@Model.Product.Price.ToString("N0") <sup><u>đ</u></sup></b>
                                </p>
                            </div>
                        </div>
                    }

                    <!-- Submit Button -->
                    <div class="btn-controls">
                        <button type="submit" class="pcart-btn">Thêm giỏ hàng</button>
                    </div>
                }
                else
                {
                    <!-- Price and Out of Stock Display -->
                    <div class="out-of-stock-container">
                        <div class="pprice-container">
                            <p class="label">Giá:</p>
                            <div class="price-controlers">
                                <p class="price main-price"><b>@(Model.Product.Price.ToString("N0"))<sup><u>đ</u></sup></b></p>
                            </div>
                        </div>
                        <div class="out-of-stock-notification">
                            <i class="fas fa-exclamation-triangle"></i>
                            <span>Hết hàng</span>
                        </div>
                    </div>
                }
            </form>
        </div>
    </div>
    <!-- Customer Reviews Section -->
    <div class="rating-section">
        <h2 style="font-size: 24px; font-weight: bold; margin-bottom: 30px;">Đánh giá từ khách hàng</h2>

        <!-- Rating Summary -->
        <div class="rating-summary">
            <div class="rating-overview">
                <!-- Average Rating -->
                <div class="average-rating">
                    <div class="average-score">@Model.AverageRating.ToString("F1")</div>
                    <div class="stars">
                        @for (int i = 1; i <= 5; i++)
                        {
                            var starClass = i <= Math.Floor(Model.AverageRating) ? "star filled" : (i == Math.Ceiling(Model.AverageRating) && Model.AverageRating % 1 >= 0.5 ? "star half-filled" : "star empty");
                            <svg class="@starClass" viewBox="0 0 24 24">
                                <path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z" />
                            </svg>
                        }
                    </div>
                    <p class="total-reviews">@Model.TotalReviews đánh giá</p>
                </div>

                <!-- Rating Distribution -->
                <div class="rating-distribution">
                    @for (int i = 5; i >= 1; i--)
                    {
                        var count = Model.RatingDistribution.ContainsKey(i) ? Model.RatingDistribution[i] : 0;
                        var percentage = Model.TotalReviews > 0 ? (count * 100.0 / Model.TotalReviews) : 0;
                        <div class="rating-row">
                            <div class="rating-label">
                                <span>@i</span>
                                <svg class="star filled" viewBox="0 0 24 24" style="width: 12px; height: 12px;">
                                    <path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z" />
                                </svg>
                            </div>
                            <div class="rating-bar">
                                <div class="rating-fill" style="width: @percentage%"></div>
                            </div>
                            <span class="rating-count">@count</span>
                        </div>
                    }
                </div>

            </div>
            <!-- Form gửi đánh giá -->
            <form asp-action="AddReview" asp-controller="Home" method="post" class="review-form">
                <input type="hidden" name="productId" value="@Model.Product.ProductID" />
                <div class="rating-field">
                    <label>Đánh giá của bạn:</label>
                    <div class="star-rating">
                        @for (int i = 1; i <= 5; i++)
                        {
                            <input type="radio" name="rating" value="6-@i" id="star-@i" required />
                            <label for="star-@i" class="star-label">★</label>
                        }
                    </div>
                </div>
                <div class="comment-field">
                    <label>Bình luận:</label>
                    <textarea name="comment" rows="4" required placeholder="Chia sẻ trải nghiệm của bạn về sản phẩm này..."></textarea>
                </div>
                <button type="submit" class="submit-review">Gửi đánh giá</button>
            </form>

            <!-- Individual Reviews -->
            <div class="reviews-list">
                @foreach (var review in Model.Reviews)
                {
                    <div class="review-item">
                        <div class="review-header">
                            <div class="review-avatar">@(review.User.UserName?.FirstOrDefault().ToString().ToUpper() ?? "N")</div>
                            <div class="review-info">
                                <div class="review-name" data-user-id="@review.User.UserID">
                                    @(review.User.UserName ?? $"Người dùng #{review.User.UserID}")
                                    <!-- User Info Tooltip -->
                                    <div class="user-info-tooltip">
                                        <div class="user-info-header">
                                            <div class="user-info-avatar">@(review.User.UserName?.FirstOrDefault().ToString().ToUpper() ?? "N")</div>
                                            <div class="user-info-name">@(review.User.UserName ?? $"Người dùng #{review.User.UserID}")</div>
                                        </div>
                                        <div class="user-info-details">
                                            <div class="user-info-row">
                                                <div class="user-info-label">Ngày tham gia:</div>
                                                <div class="user-info-value">@review.User.CreatedAt.ToString("dd/MM/yyyy")</div>
                                            </div>
                                            <div class="user-info-row">
                                                <div class="user-info-label">Cấp độ:</div>
                                                <div class="user-info-value">@review.User.Rank</div>
                                            </div>
                                            <div class="user-info-row">
                                                <div class="user-info-label">Điểm tích lũy:</div>
                                                <div class="user-info-value">@review.User.Points</div>
                                            </div>
                                        </div>
                                        <div class="user-info-stats">
                                            <div class="user-stat">
                                                <div class="user-stat-value">@review.User.TotalReviews</div>
                                                <div class="user-stat-label">Đánh giá</div>
                                            </div>
                                            <div class="user-stat">
                                                <div class="user-stat-value">@review.User.TotalOrders</div>
                                                <div class="user-stat-label">Đơn hàng</div>
                                            </div>
                                            <div class="user-stat">
                                                <div class="user-stat-value">@review.User.TotalProducts</div>
                                                <div class="user-stat-label">Sản phẩm đã mua</div>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                                <div class="review-date">@review.ReviewDate.ToString("dd/MM/yyyy")</div>
                                <div class="review-stars">
                                    @for (int i = 1; i <= 5; i++)
                                    {
                                        var starClass = i <= review.Rating ? "star filled" : "star empty";
                                        <svg class="@starClass" viewBox="0 0 24 24">
                                            <path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z" />
                                        </svg>
                                    }
                                </div>
                            </div>
                        </div>
                        <p class="review-comment">@review.Comment</p>
                    </div>
                }
            </div>
        </div>

        <!-- Pagination -->
        @if (Model.TotalPages > 1)
        {
            <div class="pagination">
                @for (int i = 1; i <= Model.TotalPages; i++)
                {
                    <a asp-action="Detail" asp-route-id="@Model.Product.ProductID" asp-route-page="@i" class="@(i == Model.CurrentPage ? "active" : "")">@i</a>
                }
            </div>
        }
    </div>
</div>

@section Scripts {
    <script>
        function showShareMessage() {
            const pshare = document.querySelector('.pshare');
            pshare.classList.add('active');
            const productUrl = `${window.location.origin}/san-pham/${@Model.Product.ProductID}`;

            navigator.clipboard.writeText(productUrl)
                .then(() => {
                    console.log("Liên kết sản phẩm đã được sao chép!");
                })
                .catch(err => {
                    console.error("Không thể sao chép liên kết: ", err);
                });
            setTimeout(() => {
                pshare.classList.remove('active');
            }, 3000);
        }

        // Quantity control logic
        var input = document.querySelector('#qty');
        var btnminus = document.querySelector('.qtyminus');
        var btnplus = document.querySelector('.qtyplus');
        var temporaryPriceDisplay = document.querySelector('#temporary-price');
        
        var pricePerUnit = @((int)(Model.Promotion != null ? Model.Promotion.NewPrice : Model.Product.Price));


        if (input !== undefined && btnminus !== undefined && btnplus !== undefined && input !== null && btnminus !== null && btnplus !== null) {
            var min = Number(input.getAttribute('min'));
            var max = Number(input.getAttribute('max'));
            var step = Number(input.getAttribute('step'));

            function updateTemporaryPrice() {
                var quantity = Number(input.value);
                var totalPrice = (pricePerUnit * quantity).toLocaleString('vi-VN');
                temporaryPriceDisplay.innerHTML = `<b>${totalPrice}<sup><u>đ</u></sup></b>`;
            }

            function qtyminus(e) {
                var current = Number(input.value);
                var newval = (current - step);
                if (newval < min) {
                    newval = min;
                    Toastify({
                        text: "Số lượng không thể nhỏ hơn " + min + "!",
                        duration: 3000,
                        gravity: "top",
                        position: "right",
                        backgroundColor: "#ff4444",
                    }).showToast();
                } else if (newval > max) {
                    newval = max;
                    Toastify({
                        text: "Số lượng không thể vượt quá " + max + "!",
                        duration: 3000,
                        gravity: "top",
                        position: "right",
                        backgroundColor: "#ff4444",
                    }).showToast();
                }
                input.value = Number(newval);
                updateTemporaryPrice();
                e.preventDefault();
            }

            function qtyplus(e) {
                var current = Number(input.value);
                var newval = (current + step);
                if (newval > max) newval = max;
                input.value = Number(newval);
                updateTemporaryPrice();
                e.preventDefault();
            }

            // Initialize the price display
            updateTemporaryPrice();

            btnminus.addEventListener('click', qtyminus);
            btnplus.addEventListener('click', qtyplus);

            // Listen for changes to the quantity input directly
            input.addEventListener('input', updateTemporaryPrice);
        }

        // Category Sidebar Toggle Logic
        const categoryButton = document.querySelector('.category-button');
        const overlay = document.getElementById('overlay');
        const containerWrapper = document.querySelector('.container-wrapper');
        const sidebar = document.querySelector('.sidebar');

        categoryButton.addEventListener('click', () => {
            overlay.classList.add('d-block');
            sidebar.classList.remove('d-none');
            containerWrapper.classList.add('blur');
        });

        overlay.addEventListener('click', () => {
            overlay.classList.remove('d-block');
            sidebar.classList.add('d-none');
            containerWrapper.classList.remove('blur');
        });

        overlay.addEventListener('mousemove', (event) => {
            if (!sidebar.contains(event.target)) {
                overlay.classList.remove('d-block');
                sidebar.classList.add('d-none');
                containerWrapper.classList.remove('blur');
            }
        });

        // Toggle subcategories
        function toggleCategorySub(categoryMain) {
            var categorySubs = document.querySelectorAll('.category-sub');

            categorySubs.forEach(function (sub) {
                if (sub !== categoryMain.nextElementSibling) {
                    sub.style.display = "none";
                }
            });

            var categorySub = categoryMain.nextElementSibling;

            if (categorySub.style.display === "none" || categorySub.style.display === "") {
                categorySub.style.display = "block";
            } else {
                categorySub.style.display = "none";
            }
        }

        function getAntiForgeryToken() {
            return document.querySelector('meta[name="csrf-token"]').getAttribute('content');
        }

        async function toggleFavorite(productId, el) {
            let isFavorited = el.dataset.isFavorited === "true";

            const url = isFavorited
                ? '/Home/RemoveFromFavorites'
                : '/Home/AddToFavorites';

            const formData = new FormData();
            formData.append("productId", productId);

            // Append the current path as returnUrl
            formData.append("returnUrl", window.location.pathname + window.location.search);

            const response = await fetch(url, {
                method: "POST",
                body: formData,
                headers: {
                    'RequestVerificationToken': getAntiForgeryToken()
                }
            });

            if (response.ok) {
                location.reload(); // or redirect manually if needed
            } else {
                alert("Có lỗi xảy ra khi gửi yêu cầu.");
            }
        }

        const qtyInput = document.getElementById("qty");
        const maxQty = parseInt(qtyInput.max, 10);
        const minQty = parseInt(qtyInput.min, 10);
        qtyInput.addEventListener('input', function () {
            let value = parseInt(this.value, 10);
            if (isNaN(value)) return;

            if (value > maxQty) {
                this.value = maxQty;
                Toastify({
                    text: "Số lượng không thể vượt quá " + maxQty + "!",
                    duration: 3000,
                    gravity: "top",
                    position: "right",
                    backgroundColor: "#ff4444",
                }).showToast();
            } else if (value < minQty) {
                this.value = minQty;
                Toastify({
                    text: "Số lượng không thể nhỏ hơn " + minQty + "!",
                    duration: 3000,
                    gravity: "top",
                    position: "right",
                    backgroundColor: "#ff4444",
                }).showToast();
            }

            // Now update the price using the corrected value
            updateTemporaryPrice();
        });

        // Load user information via AJAX when needed
        document.querySelectorAll('.review-name').forEach(nameElement => {
            const userId = nameElement.getAttribute('data-user-id');

            // Optional: Load user data via AJAX for more up-to-date information
            // This is commented out since we're using the data already in the model
            /*
            nameElement.addEventListener('mouseenter', function() {
                // Only fetch if we haven't already
                if (!this.dataset.loaded) {
                    fetch(`/api/users/${userId}/info`)
                        .then(response => response.json())
                        .then(userData => {
                            // Update tooltip with fetched data
                            const tooltip = this.querySelector('.user-info-tooltip');
                            // Update tooltip content with userData
                            this.dataset.loaded = 'true';
                        })
                        .catch(error => console.error('Error fetching user data:', error));
                }
            });
            */
        });
    </script>
}